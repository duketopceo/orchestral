"""orchestral: OpenRouter eval harness for orchestrator+worker model pairs."""

__version__ = "0.1.0"

# Human-readable benchmark-suite version — shown on share cards and reports
# so published results always carry which task set produced them.
SUITE_VERSION = "v3"
