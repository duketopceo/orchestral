id: router-eval-re-010
type: extract
prompt: |
  You are a desktop assistant decision router. Given the state, decide which
  app to open. Return ONLY a JSON object: {"choice": "<one of: browser, terminal, files, vscode>"}.

  State:
  utterance: "find the report pdf"
  focused_app: "vscode"
validation: []
assets: []
metadata:
  fields:
    choice: {type: str, required: true, enum: ["browser", "terminal", "files", "vscode"]}
  expected:
    choice: "files"
